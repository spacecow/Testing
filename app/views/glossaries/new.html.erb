<div class="form">

  <fieldset class="form">
    <legend class="form"><%= t('glossaries.new') %></legend>

    <% semantic_form_for @glossary do |f| %>
    <%= render "form", :f=>f, :label_text=>t('create') %>
    <% end %>

    <div class="links">
      <p><%= link_to t('back'), glossaries_path, :class=>"link" %></p>
    </div>

  </fieldset>
</div>
