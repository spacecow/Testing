<div class="form">
<%= error_messages_for :setting %>

<fieldset>
<legend><%= t('settings.new') %></legend>

<% form_for @setting do |f| %>
	<%= render :partial => "form", :locals => { :f=>f, :label_text=>t('create') } %>
<% end %>

<div class="links">
		<p><%= link_to t('back'), settings_path, :class=>"link" %></p>
</div>

</fieldset>
</div>