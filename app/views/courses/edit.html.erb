<div class="form">
<fieldset class="form">
<legend class="form"><%= t( 'courses.new' ) %></legend>

<div class="intro">
	<%= t('message.asterisk') %>
</div>

<% semantic_form_for @course do |f| %>
  <%= render "form", :f=>f, :label_text=>t(:create) %>
<% end %>

<div id="links">
	<%= link_to t(:show), @course if can? :show, Course %>
	<%= link_to t(:list,:object=>t('courses.title')), courses_path if can? :index, Course %>
</div>

</fieldset>
</div>