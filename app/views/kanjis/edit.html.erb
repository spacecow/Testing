<div class="form">
<%= error_messages_for :kanji %>

<fieldset>
<legend><%= t('kanjis.editing') %></legend>

<% form_for @kanji do |f| %>
	<%= render :partial => "form", :locals => { :f=>f, :label_text=>t('update') } %>
<% end %>

<div class="links">
		  <%= link_to t('show'), @kanji, :class=>"link" %> |
		  <%= link_to t('view_all'), kanjis_path, :class=>"link" %>
		</p>
</div>

</fieldset>
</div>