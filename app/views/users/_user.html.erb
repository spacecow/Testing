<tr class="<%= cycle( 'list-line-odd', 'list-line-even' ) %>" id="user_<%= user.id %>">
	<td><%=h user.username %></td>
	<td><%=h user.name %></td>
	<td><%=h user.name_hurigana %></td>
	<td><%=h user.email %></td>	
	<td><%=h user.tel %></td>
	<td id="links">
		<% if permitted_to? :show, user %>
	  	<%= link_to t('show'), user %>
	  <% end %>
		<% if permitted_to? :edit, user %>
	  	<%= link_to t('edit'), edit_user_path(user) %>
	  <% end %>
		<% if permitted_to? :edit_role, user %>
	  	<%= link_to t(:role), edit_role_user_path( user ) %>
	  <% end %>
		<% if permitted_to? :destroy, user %>
	  	<%= link_to t('delete'), user, :confirm => 'Are you sure?', :method => :delete %>
	  <% end %>
	</td>  
</tr>