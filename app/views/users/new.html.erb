<div class="form">
<%= error_messages_for :user %>

<fieldset>
<legend><%= t('users.new') %></legend>

<% semantic_form_for @user do |f| %>
	<%= render :partial => "form", :locals => { :f=>f, :label_text=>t('create') } %>
<% end %>

<div class="links">
</div>

</fieldset>
</div>