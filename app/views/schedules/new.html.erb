<div class="form">
<%= error_messages_for :schedule %>

<fieldset>
<legend><%= t('schedules.new') %></legend>

<% form_for @schedule do |f| %>
	<%= render :partial => "form", :locals => { :f=>f, :label_text=>t('create') } %>
<% end %>

<div class="links">
		<p><%= link_to t('back'), schedules_path, :class=>"link" %></p>
</div>

</fieldset>
</div>